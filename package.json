{
  "name": "linikers-portfolio",
  "private": true,
  "scripts": {
    "build": "cd linikers && npm install --legacy-peer-deps && next build",
    "postinstall": "cd linikers && npm install --legacy-peer-deps"
  },
  "dependencies": {
    "next": "13.4.3",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
